# 目前的进度

更新时间：2026-08-11

这个文档是给下一轮继续开发/测试用的交接记录。它整理了本次对话里的目标、已经完成的改动、手机实测状态、工作节奏，以及下一步应该怎么接着做。

## 项目位置和当前版本

- 主项目目录：`F:\AI\WiFinder`
- 开发工作副本：`C:\Users\RichieC\Documents\Codex\2026-08-09\files-mentioned-by-the-user-rcha585\work\WiFinder`
- 当前分支：`feature/wifinder-anchor-mvp`
- 最新提交：`1cb1891 feat: add Huawei AR Engine anchor runtime`
- Android 当前安装版本：`versionName=1.1-anchor-huawei`，`versionCode=2`
- 当前手机：Huawei `NAM-AL00`，ADB 设备号 `EMH0222111031083`

`F:\AI\WiFinder` 已同步到最新提交。因为 F 盘文件系统不记录 Git ownership，读写 Git 状态时如果遇到 `dubious ownership`，可以临时使用：

```powershell
git -c safe.directory=F:/AI/WiFinder status
```

## 这轮开发的核心目标

原目标是基于现有 `rcha585/WiFinder` 做一个新的实验产品版本：`WiFinder Anchor - MVP 1`。

它和旧 WiFinder v1 的关系是：

- v1 的 `Main App`、`Scan Tool`、Building 302 地图、Random Forest 定位逻辑都保留，不合并进新逻辑。
- 新功能入口是 `Anchor Mapping (Experimental)`。
- 新逻辑用三台以上浏览器设备显示视觉 anchor，再用 Android 手机做 AR 扫描、Wi-Fi 扫描和空间数据导出。
- 目标不是直接复用旧 Building 302 坐标，而是生成新的米制空间数据集和 2D floor plan 预览。

## 已经完成的主要工作

### 1. Anchor Web

新增/完善了 `anchor-web/`：

- 用浏览器生成黑白视觉 marker。
- marker 由 `sessionId + anchorId` 决定，所以 Android 端可以动态生成同样的识别数据库。
- 当前 session 示例：`D8747AA6`
- 当前浏览器曾打开过：`http://192.168.0.192:4173/?session=D8747AA6&anchor=A`
- 链接支持 Anchor A/B/C/D。
- 无账号、无云后台、无数据库。

现在网页还增加了设备预设：

- Anchor A：14 inch laptop Chrome，默认 marker width `14.0 cm`
- Anchor B：iPhone 14 Pro Safari，默认 marker width `5.4 cm`
- Anchor C：13 inch iPad Pro Safari，默认 marker width `16.0 cm`

这些预设是为了第一轮快速测试不用尺子。它们不是物理真值，因为浏览器工具栏、系统缩放、页面缩放、是否全屏都会影响真实显示尺寸。后续如果要做严谨实验，还是建议量一下实际 marker 边长。

### 2. Android Anchor Mapping

Android 端已实现：

- 新入口：`Anchor Mapping (Experimental)`
- 三个 anchor 校准状态：`A/B/C: waiting` 或 `detected`
- 默认 A/B/C 三个设备预设尺寸，不再统一用一个 14 cm
- 可以动态生成 Google ARCore 和 Huawei AR Engine 两套 augmented image database
- Mapping 阶段记录：
  - AR camera pose
  - anchor observation
  - plane hit-test / vertical plane
  - Wi-Fi scan sample
  - 轨迹和基础 2D floor plan 数据
- 导出：
  - `mapping.json`
  - `floorplan.svg`

### 3. 华为手机兼容

用户手机是华为，旧版本打开 AR 时直接进入 Google ARCore 安装流程，导致截图里出现：

```text
ARCore could not start:
UnavailableUserDeclinedInstallationException
```

原因是旧代码在 `AnchorMappingActivity` 里直接调用了 Google ARCore 的 `requestInstall()`。它不是 WiFinder 自己要登录 Google，也不是 WiFinder 上传数据，而是 Google ARCore 的本地空间定位引擎需要 `Google Play Services for AR` 作为运行时。

现在已经改成：

- 华为手机优先使用 `Huawei AR Engine`
- 其他 Android 设备回退到 `Google ARCore`
- 不再在这台华为手机上主动弹 Google ARCore 安装
- Huawei 路径显示为：`Huawei AR Engine plane tracking enabled; depth points unavailable`

这台手机上已确认存在华为运行时包：

```text
package:com.huawei.arengine.service
```

## 手机实测状态

已经完成：

1. 连接手机成功：

```text
EMH0222111031083 device product:NAM-AL00 model:NAM_AL00 device:HWNAM
```

2. 覆盖安装新版 APK 成功：

```text
Performing Streamed Install
Success
```

3. 安装后确认：

```text
versionCode=2
versionName=1.1-anchor-huawei
CAMERA: granted=true
ACCESS_FINE_LOCATION: granted=true
```

4. 已进入主 App，并从按钮进入：

```text
Anchor Mapping (Experimental)
```

5. 设置页确认默认尺寸：

```text
Anchor A width = 14.0 cm
Anchor B width = 5.4 cm
Anchor C width = 16.0 cm
```

6. 输入 session `D8747AA6` 后点击 `Step 1 - Calibrate Anchors`，成功进入校准页。

7. 当前校准页显示：

```text
A: waiting
B: waiting
C: waiting
Huawei AR Engine plane tracking enabled; depth points unavailable
```

这说明新版已经绕开 Google ARCore，并启动了 Huawei AR Engine 路径。下一步需要用户拿手机对准三块 anchor 屏幕，观察 A/B/C 是否变成 `detected`。

日志里有一些 Huawei HiAR 内部 warning，例如 `WaitAvaliablePreview`、`GetDisplayOrientedPose`、`HSDK_AUGMENTED_IMPL_UPDATE_ANCHOR_LIST not found`。目前它们没有导致 App 崩溃，也没有阻止进入校准页。后续如果 marker 一直识别不到，再重点看这些日志。

## 已跑过的验证

Web：

```powershell
cd anchor-web
npm test
npm run build
```

结果：

- Web tests：4 个通过，0 失败
- `anchor-web/dist/` 构建成功

Android：

```powershell
$env:JAVA_HOME='C:\Program Files\Java\jdk-21'
$env:Path="$env:JAVA_HOME\bin;$env:Path"
./gradlew.bat testDebugUnitTest assembleDebug lintDebug
```

结果：

- Android unit tests：14 个通过，0 失败
- `assembleDebug` 成功
- `lintDebug` 成功
- APK 路径：`app/build/outputs/apk/debug/app-debug.apk`

注意：Gradle daemon 如果用 JDK 24，会在 `testDebugUnitTest` 阶段报：

```text
Type T not present
```

解决方式是停掉 daemon，并用 JDK 21 运行：

```powershell
./gradlew.bat --stop
$env:JAVA_HOME='C:\Program Files\Java\jdk-21'
$env:Path="$env:JAVA_HOME\bin;$env:Path"
./gradlew.bat testDebugUnitTest
```

## 当前建议测试流程

下一轮直接从这里继续：

1. 保持 Anchor Web server 运行。
2. 准备三台设备：
   - A：14 寸电脑 Chrome
   - B：iPhone 14 Pro Safari
   - C：13 寸 iPad Pro Safari
3. 三台设备打开同一个 session 的 A/B/C 链接。
4. 三台设备固定在房间里，尽量不共线，屏幕亮度调高。
5. 手机保持在当前 `Anchor Mapping` 校准页。
6. 依次让手机摄像头看到 A、B、C 的完整 marker。
7. 观察状态是否从 `waiting` 变成 `detected`。
8. 三个都 detected 后点击 `Start Mapping`。
9. 沿房间边界慢慢走一圈，镜头斜扫墙面和墙角。
10. 回到起点附近，再让至少一个 anchor 进入画面，然后点击 `Finish Scan`。
11. 查看 2D preview 和导出的 `mapping.json` / `floorplan.svg`。

## 如果识别不到 marker，优先排查这些

1. 网页 session 是否和 Android 输入一致。
2. Anchor A/B/C 链接是不是同一个 session，但不同 anchor。
3. 浏览器页面是否完整显示黑白 marker，没有被工具栏或缩放裁掉。
4. 屏幕亮度是否足够，是否反光。
5. 手机是否距离太近或太远。
6. 是否需要先让摄像头在房间里轻微移动，让 Huawei AR Engine 建立 tracking。
7. 如果一直无法识别，考虑做一个 Huawei-only 的降级分支：先用 `ARImageTrackingConfig` 只测试图像识别，再决定是否能和 world tracking/plane tracking 合并。

## 工作节奏记录

这轮的节奏大概是：

1. 先保留旧功能，不动 v1 主流程。
2. 新功能独立放在 `Anchor Mapping (Experimental)`。
3. 先把 Web anchor 和 Android AR 流程跑通。
4. 发现华为手机无法装 Google ARCore 后，检查手机运行时。
5. 确认手机有 `com.huawei.arengine.service`。
6. 加 Huawei AR Engine SDK 和 Maven 源。
7. 用官方 sample / SDK class signature 确认 API。
8. 写 Huawei runtime 适配层。
9. 保留 Google ARCore fallback。
10. 修复窄屏 UI，把 A/B/C chip 从横向挤压改成可换行布局。
11. 把统一 marker width 改成 A/B/C 独立宽度。
12. 根据用户要求加入常见设备预设，减少第一轮测试的手工测量。
13. 跑 Web 和 Android 验证。
14. 同步到 `F:\AI\WiFinder`。
15. 覆盖安装到手机。
16. 进入真机校准页验证 Huawei AR Engine 路径。

后续建议还是保持这个节奏：先做小闭环，真机验证，再扩大范围。AR 这块不要一次性做太多假设，因为设备、浏览器、光照、系统运行时都会影响结果。

## 下一轮最适合做的事

下一轮不要先大改代码，先做真实 marker 识别测试：

- 如果 A/B/C 能 detected：继续做完整 mapping 流程，检查导出结果。
- 如果只有部分 detected：记录是哪台设备、哪个 preset、距离/角度/亮度。
- 如果完全不能 detected：优先调 Huawei AR Engine 的 image tracking 配置，而不是先动 floor plan 或 Wi-Fi。
- 如果能 detected 但 floor plan 很差：再看 plane hit-test、trajectory drift、墙线拟合参数。

## 重要文件索引

- `anchor-web/src/app.js`：Web session、anchor link、preset 参数逻辑
- `anchor-web/src/presets.js`：A/B/C 默认设备尺寸预设
- `app/src/main/java/com/example/compsci399testproject/anchor/ui/AnchorMappingActivity.kt`：AR runtime 选择，Huawei 优先、Google fallback
- `app/src/main/java/com/example/compsci399testproject/anchor/ar/AnchorArRuntime.kt`：runtime 抽象
- `app/src/main/java/com/example/compsci399testproject/anchor/ar/HuaweiArSurfaceView.kt`：Huawei camera surface
- `app/src/main/java/com/example/compsci399testproject/anchor/ar/HuaweiArFrameExtractor.kt`：Huawei frame 转换成统一 `ArFramePacket`
- `app/src/main/java/com/example/compsci399testproject/anchor/ar/MarkerBitmapFactory.kt`：Google/Huawei 两套 image database，A/B/C 独立宽度
- `app/src/main/java/com/example/compsci399testproject/anchor/ui/AnchorMappingScreen.kt`：Android UI，预设尺寸输入和校准页
- `app/src/main/java/com/example/compsci399testproject/anchor/ui/AnchorMappingViewModel.kt`：session 准备、状态、记录流程
- `docs/WIFINDER_ANCHOR_MVP.md`：当前 MVP 说明文档

## 参考资料

- Huawei AR Engine demo: https://github.com/HMS-Core/hms-AREngine-demo
- Huawei AR Engine codelab: https://developer.huawei.com/consumer/en/codelab/HWAREngine/index.html
- iPhone 14 Pro specs: https://support.apple.com/en-la/111849
- iPad Pro specs: https://www.apple.com/ipad-pro/specs/
